# knowhowseller — AI 영상 포트폴리오

`knowhowseller.com/portfolio` 에서 이 저장소의 HTML을 가져와 출력한다.
워드프레스 REST 인증이 호스팅에서 막혀 있어, 이 저장소를 콘텐츠 원본으로 쓴다.

- `portfolio.html` — 포트폴리오 본문 (생성기: `build_portfolio.py`)
- 갱신: 파일을 다시 만들어 push하면 사이트에 반영된다(캐시 10분)

자격증명은 이 저장소에 넣지 않는다.
